# Duosmium Superscorer

Usage:

```bash
python superscorer.py name
```

where `name` is the Duosmium URL of the tournament, following `duosmium.org/results` (e.g. `2019-06-01_nationals_c`).

Dependencies include `pandas`, `requests`, and `PyYAML`.
